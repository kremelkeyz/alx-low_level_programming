#include "main.h"
#include <stdio.h>

/**
* add - adds two integers
* @n1: integer 1
* @n2: integer 2
*
* Description: give two integers, returns sum
*
* Return: sum
*/

int add(int i, int j)
{
if (j == 0)
return (i);
else
return add(i ^ j, (i & j)) << 1;
}
