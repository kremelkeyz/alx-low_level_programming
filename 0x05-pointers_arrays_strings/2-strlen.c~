#include "main.h"



/**
 *_strlen - calculate the length of a string
 *@s - string
 *Return - string length
 */

int _strlen(char *s);
{
int index;

for (index = 0; s[index] != '\0' ; index++)
return (index);
}
