#include "main.h"
#include <stdio.h>
/**
 *_puts - a function taht prints a string followed by anew line
 *@str: string input
 *Return: 0
 */

void _puts(char *str)
{
int index;

for (index = 0; s[index] != '\0' ; index++)

{
_putchar(str[index]);
}
_putchar(\n);
}
